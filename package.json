{
  "name": "phrouros",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phrouros — local monitoring dashboard for OpenCode and oh-my-openagent (single Bun process, read-only SQLite)",
  "bin": {
    "phrouros": "./src/server.ts"
  },
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --hot run src/server.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
